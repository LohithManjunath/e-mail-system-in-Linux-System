#ifndef _WINDOW_H
#define _WINDOW_H

#include<gtk/gtk.h>
GtkWidget * new_welcom_window();
GtkWidget * new_login_window();
GtkWidget * new_register_window();
GtkWidget * new_main_window();
GtkWidget * new_send_window();
GtkWidget * new_change_psw_window();
gint settime(gpointer );

#endif
